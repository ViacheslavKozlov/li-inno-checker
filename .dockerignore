# Keep the build context (and Railway upload) small + reproducible.
node_modules
dist
.git
.github
.claude
*.log
logs
coverage

# Local env / secrets — never bake these into an image
.env
.env.*
!.env.example

# Editor / OS cruft
.DS_Store
.vscode
.idea

# Docs not needed at build time
*.md
